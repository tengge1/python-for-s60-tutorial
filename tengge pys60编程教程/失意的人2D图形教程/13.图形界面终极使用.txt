本教程来自iniwap！转载请注明出处！
作者：┈┾夨憶D*.亼ぺ
#当你看到这时，这已经是第十三课了
#这一课可谓是犹抱琵琶半遮面，千呼万唤始出来啊
#怎么样？前面的学会了吗？
#不懂的一定要发问哦。
#下面我们将要学习的是图形界面中
#最牛X的一个函数blit的用法
#学会他，你将能写出很炫的程序，记住是很炫哦
#首先我们看下blit的神秘原型是什么样的
#blit(image,target=(0,0),source=(0,0,image.size),mask=None,scale=0)
#是不是有点晕了？其实很多部分，前面的课程已经涉及到#所以大家一定要多看多练习，多尝试
好了，废话不多说，请看：
#blit怎么用各位一定很清楚了从第一课就有img.blit()
#不再赘述
#下面介绍各个参数的含义及用法
#image:图像，什么图像呢？你打开的图像或者你新建的图像，还记得那个new和open吗？
#不记得的去看前面的教程
#target:目标，顾名思义，就是你要把图像要放到的位置
#()里的参数含义应该清楚了吧？就是image的左上顶点的x,y坐标，默认为(0,0)即左上角
#下面看source：源，很明显是指你打开的图片，那括号里的参数都代表什么呢？
#前两个同样代表左上角，但是指得是图像上的坐标，而上
#面的坐标呢？对，就是img.blit的img的坐标(img可以是
#屏幕也可以是另一张图，这样就形成了图上加图了不是
#吗)，image.size?这个不陌生吧？前面讲了的，获得图片
#的(宽,高)。(0,0,image.size)代表了什么呢？当然是整
#个图喽。这也是默认的，如果你需要显示的只是图的一
#部分，尝试改变四个值吧。
#mask:这个又是什么呢？高级了点，有时候你要显示到另
#一张图的图片不是透明的，会很难看，怎么办呢？
#这个mask就是为了产生透明图像的。
#那mask有什么要求呢？必须是和原图一样大小而且是黑白图哦。其他和image一样。
#scale：范围，是否缩放，用处不大，忽略。
#是不是有点晕了呢？
#下面来点图文说明吧
--------
共有9个附件:
这个是教程中所讲的实例的三个图了。第一个img.blit的img也就是背景，这个背景可以是图片，也可以是new函数创建的图哦
第二个就是要显示到背景上的图，第三个是mask
这个示意img.blit(image)也就是将image添加到img上（显示）
这张是target=(0,0)的效果喽。很明显image被画到了img的左上角，这就是（0，0）的含义。
这张很明显是target=(0,50)。
看看是不是被画到了背景的(0,50)处？
这个示意为source=(60,0,165,78)，看清楚箭头了吗？这样的话只把第二张图（开头说的）的坐标为（60，0，165，78）的部分，应该是五角星
看看这效??，上不上只画了五角星？并且在背景的（0，50）处？
这个又添加了mask
看看五角星是不是透明了？白色的没了吧?好看吧？只支持png哦貌似。呵呵~终于讲完了，尊重下我的劳动成果哦。好好学吧。慢慢体会！
