#不妨设加密脚本的名称为tengge.pyc

1.先新建一个新脚本，名称任意，内容为import tengge。
2.用新脚本按前面讲的方法打包。
3.用智能解包的解包功能解包打包好的软件，并将tengge.pyc放入“private\\你打包软件的UID号\\”下。
4.重新打包软件即可。

注：你把“private\\你打包软件的UID号\\tengge.pyc”放到附加目录中也行。这样只要打包一遍就行了。