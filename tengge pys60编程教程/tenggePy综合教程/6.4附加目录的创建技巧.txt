请先找一个空文件夹备用。然后看下面的例子。

1.将“1.txt”打包到“c:\\System\\apps\\”下。

方法：在空文件夹中建一个名称为“c”的文件夹，再在名称为“c”的文件夹中建一个名称为“System”的文件夹，以此类推，直至建到“apps”的文件夹，并将“1.txt”放到里面。

2.将“1.txt”打包到“用户安装该软件的盘符\\System\\apps\\”下。

方法：只需将上面的“c”改为“!”（在英文状态下输入）即可。
