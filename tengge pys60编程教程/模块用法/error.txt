
这个模块很简单！主要是引发程序错误提示！直接引用模块即可：
import error
如果在调用过程中如内存不足或是其它语法错误会引发该模块。正常都是放在末尾的！
顺便在提一下。一般情况下我们要知道某个模块的用法可以这样！
import 模块名
dir(模块名) [明天见最后编辑于2008-11-6 2:25:37]改名error.py(不分大小写)放入!/System/libs/目录下！(或是工作目录下)
--------
